query users($page: Int, $pageSize: Int, $messageType: Int) {
  users(page: $page, pageSize: $pageSize, messageType: $messageType) {
    username
    reserved1
    reserved2
    conRemark
    nickname
  }
}
